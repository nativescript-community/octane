/**
 * What TypeScript resolves for `"jsxImportSource": "@nativescript-community/octane"`:
 * the `JSX` namespace of `./intrinsics`. Octane compiles the JSX itself, so
 * nothing here runs; the module exists for the types.
 */
export * from './intrinsics.js';
